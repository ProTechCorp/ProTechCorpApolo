spring:
  datasource:    url: jdbc:postgresql://localhost:5432/ProTechCorp
    username: postgres
    password: angelo11
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:      ddl-auto: update
    generate-ddl: true
    show-sql: true 